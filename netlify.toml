[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Make sure the React app works on refresh / deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
